<% if current_user.following? artist %>
    <%= button_to "unFollow", unfollow_artist_path(artist), method: :delete, remote: true %>
<% else %>
    <%= button_to "Follow", follow_artist_path(artist), remote: true %>
<% end %>
